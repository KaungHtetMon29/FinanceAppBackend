import { ResStatusInterceptor } from './res_status.interceptor';

describe('ResStatusInterceptor', () => {
  it('should be defined', () => {
    expect(new ResStatusInterceptor()).toBeDefined();
  });
});
