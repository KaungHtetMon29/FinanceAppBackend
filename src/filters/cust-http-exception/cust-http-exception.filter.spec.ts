import { CustHttpExceptionFilter } from './cust-http-exception.filter';

describe('CustHttpExceptionFilter', () => {
  it('should be defined', () => {
    expect(new CustHttpExceptionFilter()).toBeDefined();
  });
});
